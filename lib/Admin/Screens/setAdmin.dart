import 'package:flutter/material.dart';

class setAdmin extends StatelessWidget {
  const setAdmin({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
